# Use Alpine Linux as our base image.
# Alpine is very lightweight (approx 5MB), making it perfect for basics.
FROM alpine:latest

# This is the command that runs when the container starts.
# We are simply using the shell 'echo' command to print text.
CMD ["echo", "Hello, World! Welcome to your first Docker container."]
